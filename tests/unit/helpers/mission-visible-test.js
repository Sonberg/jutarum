import { missionVisible } from 'client/helpers/mission-visible';
import { module, test } from 'qunit';

module('Unit | Helper | mission visible');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = missionVisible([42]);
  assert.ok(result);
});
