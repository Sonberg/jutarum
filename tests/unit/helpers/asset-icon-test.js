import { assetIcon } from 'client/helpers/asset-icon';
import { module, test } from 'qunit';

module('Unit | Helper | asset icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assetIcon([42]);
  assert.ok(result);
});
