import helper from 'client/utils/helper';
import { module, test } from 'qunit';

module('Unit | Utility | helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helper();
  assert.ok(result);
});
