import { team } from 'client/helpers/team';
import { module, test } from 'qunit';

module('Unit | Helper | team');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = team([42]);
  assert.ok(result);
});
