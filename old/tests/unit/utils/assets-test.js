import assets from 'client/utils/assets';
import { module, test } from 'qunit';

module('Unit | Utility | assets');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assets();
  assert.ok(result);
});
