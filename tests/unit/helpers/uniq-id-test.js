import { uniqId } from 'client/helpers/uniq-id';
import { module, test } from 'qunit';

module('Unit | Helper | uniq id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = uniqId([42]);
  assert.ok(result);
});
